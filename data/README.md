# Data Folder Note

This project keeps its working datasets in `datasets/` to preserve the current data pipeline.

- Raw/working inputs: `datasets/`
- Processed file used by the app: `datasets/processed.json`
- Transform script: `scripts/generate-processed-data.mjs`

This `data/` folder is included for demo-day repository structure clarity.
